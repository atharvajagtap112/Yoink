<?xml version="1.0" encoding="utf-8"?>
<!--
  Landmarks-only view: no camera preview, just the skeleton drawn on black. The
  camera still runs (the analyzer feeds MediaPipe) — we simply never attach its
  surface, so nothing of the image is shown. Less distracting, and it doesn't
  broadcast a live view of your face on screen.
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/black">

    <com.yoink.OverlayView
        android:id="@+id/overlay"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:orientation="vertical"
        android:background="#B0000000"
        android:padding="12dp">

        <TextView
            android:id="@+id/status"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="@android:color/white"
            android:text="@string/starting" />

        <Button
            android:id="@+id/camera_toggle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/camera_off" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal">

            <Button
                android:id="@+id/overlay_perm"
                android:layout_width="0dp"
                android:layout_weight="1"
                android:layout_height="wrap_content"
                android:text="@string/grant_overlay" />

            <Button
                android:id="@+id/enable_capture"
                android:layout_width="0dp"
                android:layout_weight="1"
                android:layout_height="wrap_content"
                android:text="@string/enable_capture" />

            <Button
                android:id="@+id/bal_test"
                android:layout_width="0dp"
                android:layout_weight="1"
                android:layout_height="wrap_content"
                android:text="@string/bal_test" />
        </LinearLayout>
    </LinearLayout>
</FrameLayout>
